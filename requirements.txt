# SolarGrid Advisor backend — Python dependencies (LIGHT / low-RAM build)
# Install:  pip install -r requirements.txt
#
# Lightweight variant: keyword (BM25) retrieval only, no torch. It idles well
# under 100 MB RAM, so it fits small hosts (e.g. Railway trial). Everything
# works — chat, citations, filters, bills, calculators, scanned-PDF OCR —
# except vector (semantic) search, which needs the full build below.
# (OCR of very large scans briefly uses a few hundred MB while it runs.)
#
# FULL BUILD (local / bigger server, vector search + reranker):
#   pip install sentence-transformers>=3.0 faiss-cpu>=1.8
#
# NOTE: OCR needs the Tesseract *system* package, installed on Railway via
# nixpacks.toml. Locally (Ubuntu/Debian):
#   sudo apt install tesseract-ocr tesseract-ocr-eng tesseract-ocr-urd

# --- Web framework ---
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# --- LLM client (OpenAI-compatible: xAI Grok / Modal gpt-oss-120b / Ollama) ---
openai>=1.40

# --- PDF text extraction + OCR for scanned PDFs ---
pdfplumber>=0.11
pypdf>=4.0
pypdfium2>=4.0
pytesseract>=0.3.10
Pillow>=10.0

# --- Data ---
numpy>=1.24
